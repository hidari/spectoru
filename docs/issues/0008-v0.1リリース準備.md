# [Phase 9] v0.1 リリース準備

親: [0001](./0001-v0.1リリースまでのロードマップ.md)
依存: [0007](./0007-CLI配線とE2Eテスト.md)

## 目的

動くツールを、他人が使える状態にする。

## README

現状 `# spectoru` の 1 行のみ。以下を書く。

- 何を解決するツールか（テストを仕様として可視化する）
- インストール方法（`cargo install --git`。crates.io 公開は v0.1 スコープ外）
- `spec-site.toml` の書き方
- 4 つのサブコマンドの使い方
- 「仕様としてのテスト」の書き方の指針（日本語テスト名の例を含む）
- 単一リポジトリ / 複数リポジトリ集約の CI 設定例
- 生成サイトのスクリーンショット

## ドッグフーディング

spectoru 自身のテストを spectoru でサイト化する。

- リポジトリルートに `spec-site.toml` を置く
- CI でサイトを生成し、成果物として確認できるようにする
- **これが最も実効性のある回帰テストになる**。日本語テスト名、ネストした
  `mod tests`、契約テストと統合テストの混在という、まさに想定ユースケースに
  近い構成を自前で持っている

## リリース workflow

CLAUDE.md は「ローカルからのリリースは絶対に行わない」と定めているが、
その受け皿となる GitHub Actions が存在しない。

- タグ push をトリガーにバイナリをビルドして Release に添付する
- 対象プラットフォームを決める（最低限 `x86_64-unknown-linux-gnu`。
  macOS / Windows を含めるかは需要次第）
- `permissions` は最小限にし、既存 workflow と同様に action を
  コミット SHA でピン留めする
- リリース前に `cargo xtask ci` と `cargo xtask deny` を通す

## その他

- ブラウザ E2E（0006 → 0007 からの持ち越し）を入れるか判断する。node と
  Playwright を CI に持ち込むコストに見合うか。現状はエスケープと外部依存ゼロを
  CLI の E2E で文字列レベルまで検証しており、ブラウザでの確認は手動実施済み
- `docs/planning/方向性.md` と実装の乖離がないか最終確認する
- 実装過程で判明した設計判断を方向性ドキュメントに反映する
- v0.1 スコープ外とした項目を将来 issue として起票するか判断する

## 完了条件

- [ ] README が単体で使い方を理解できる内容になっている
- [ ] spectoru 自身の仕様サイトが CI で生成されている
- [ ] リリース workflow でタグから成果物が作られる
- [ ] v0.1 タグを打つ
