# [Phase 9] v0.1 リリース準備

親: [0001](./0001-v0.1リリースまでのロードマップ.md)
依存: [0007](./0007-CLI配線とE2Eテスト.md)

## 目的

動くツールを、他人が使える状態にする。

## README（実施済み）

`# spectoru` の 1 行から、単体で使い方を理解できる内容に書き換えた。

- 解決したい課題と名前の由来
- インストール方法（`cargo install --git`。crates.io 公開は v0.1 スコープ外）
- クイックスタート（最小の `spec-site.toml` と `spectoru build`）
- 「仕様としてのテスト」の書き方（Rust / Vitest それぞれの規則と、
  `mod tests` の除去や修飾子の解釈といった挙動）
- 設定ファイルの全項目
- 4 つのサブコマンドと終了コードの意味
- 単一リポジトリ / 複数リポジトリ集約の CI 設定例
- 開発コマンドとアーキテクチャの説明
- スクリーンショット（spectoru 自身の仕様サイト）

## LICENSE（実施済み）

`Cargo.toml` が `MIT OR Apache-2.0` を宣言しているのにリポジトリに本文が
無かった。`LICENSE-MIT` と `LICENSE-APACHE` を追加し、`cargo xtask dist` の
アーカイブにも同梱するようにした（両ライセンスとも配布物に著作権表示と
条文を含めることを条件にしているため）。

本文は記憶からの書き起こしではなく、Rust ツールチェーンに同梱されている
正典（`share/doc/rust/licenses/`）を使った。2 つのツールチェーンの間で
ハッシュが一致することも確認している。

著作権表示は `Cargo.toml` の `authors = ["Hidari"]` に合わせて
`2026 Hidari` とした。法人名などで表記したい場合は `LICENSE-MIT` の 1 行を
書き換える。

## ドッグフーディング（実施済み）

spectoru 自身のテストを spectoru でサイト化した。

- リポジトリルートに `spec-site.toml` を置いた。source は「ユニットテスト」
  （`src/`）と「契約テスト・統合テスト」（`tests/`）の 2 つ
- `cargo xtask spec-site` で `dist/` に生成する。`--strict` 付きなので、
  自分の仕様が自分の品質基準を満たさなくなった時点で落ちる
- CI に `spec-site` ジョブを追加し、生成できることと外部リソースを読み込んで
  いないことを検証する

結果は **239 spec / 警告 0**。`cargo xtask ci` が数えるテスト数と一致した。

### ドッグフーディングで見つかった設計の穴: 除外の仕組みが無い

`crates/spectoru/tests/fixtures/` にはパーサへの入力データが入っている。
これらは spectoru の仕様ではないのでサイトに載ると内容として誤りになるうえ、
意図的に構文を壊したファイルまで含まれており error になっていた。

`.gitignore` に載っていない「コミットされているが仕様ではないもの」を外す
手段が無いという穴で、フィクスチャや生成コードを持つプロジェクトなら
どこでも踏む。`[[sources]].exclude` を追加して解決した（0004 の想定にも
方向性ドキュメントにも無かった項目）。

### 残っている作業

CI での成果物アップロードは未実施。`actions/upload-artifact` を使うには
コミット SHA でのピン留めが必要で、作業環境から解決できなかった。
デプロイ先（方向性ドキュメントでは Cloudflare Pages）の設定と併せて行う。

## リリース workflow（実施済み）

`v*.*.*` のタグ push をトリガーに、検証 → 下書き作成 → 成果物追加 → 公開の
4 ジョブで進む。`cargo xtask dist <target>` が配布用アーカイブを作る。

対象は `x86_64-unknown-linux-gnu` と `aarch64-apple-darwin` の 2 つ。CI が
Linux、ローカル開発が macOS という想定に対応する。Windows は CI での
ビルド実績が無く、初めてのビルドがリリース本番になるのを避けたいため見送った。

### 決定: 成果物のアップロードにサードパーティ製アクションを使わない

GitHub ランナーには `gh` CLI がプリインストールされているため、リリースの
作成・アップロード・公開はすべて `gh release` で完結する。ピン留めして追跡
すべき依存が増えないぶん、サプライチェーン上の面積が小さくなる。

### 決定: 下書きを経由してから公開する

先に `--draft` で作り、各プラットフォームの成果物が揃ってから公開する。
ビルドが 1 つでも失敗したときに、中途半端なリリースが利用者から見える状態に
ならないようにするため。`fail-fast: false` にしているのは、どのプラット
フォームまで通ったかが分かる方が原因を絞りやすいから。

### 決定: タグと Cargo.toml のバージョン一致を検証する

ずれたままリリースすると、配布物の `--version` が名乗る番号とタグが食い違う。
公開後には直せないので、検証ジョブの最初に落とす。

### 残っている作業

- CI での仕様サイトのアップロードとデプロイ（Cloudflare Pages）。
  `actions/upload-artifact` をコミット SHA でピン留めする必要がある

## その他

- ブラウザ E2E（0006 → 0007 からの持ち越し）を入れるか判断する。node と
  Playwright を CI に持ち込むコストに見合うか。現状はエスケープと外部依存ゼロを
  CLI の E2E で文字列レベルまで検証しており、ブラウザでの確認は手動実施済み
- `docs/planning/方向性.md` と実装の乖離がないか最終確認する
- 実装過程で判明した設計判断を方向性ドキュメントに反映する
- v0.1 スコープ外とした項目を将来 issue として起票するか判断する

## 完了条件

- [x] README が単体で使い方を理解できる内容になっている
- [x] spectoru 自身の仕様サイトが CI で生成されている
- [x] リリース workflow でタグから成果物が作られる
- [x] LICENSE 本文がリポジトリと配布物に含まれている
- [ ] v0.1 タグを打つ

タグを打つのは残りの確認（方向性ドキュメントと実装の最終突き合わせ、
CI がすべて緑であること）が済んでから。
