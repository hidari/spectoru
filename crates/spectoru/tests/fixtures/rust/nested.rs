// ネストした統合テスト。条件ごとに mod で分ける。

mod 有効な画像がアップロードされたとき {
    #[tokio::test]
    async fn 作品が公開状態で作成される() {}

    #[tokio::test]
    async fn コラボレーターにクレジットが付与される() {}
}

mod タイトルが未入力のとき {
    mod さらに説明も未入力のとき {
        #[test]
        fn バリデーションエラーが返される() {}
    }
}

mod テストを含まないヘルパー {
    pub fn build() {}
}
